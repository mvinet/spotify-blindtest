{
  "name": "spotify-blindtest",
  "version": "1.0.0",
  "description": "Blind Test with spotify playlist",
  "private": true,
  "author": "mvinet",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "build": "cd client && yarn install && yarn build && cd .. && yarn start"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^3.1.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^14.14.31",
    "typescript": "^4.2.2"
  }
}
